# Only what the shipped scripts actually import. Tested on Python 3.13.
numpy>=2.0,<3
pandas>=2.2,<3
scipy>=1.13,<2
matplotlib>=3.8,<4
python-docx>=1.1      # only for the submission documents, not for the analysis
